# ╔══════════════════════════════════════════════════════════════════════════╗
# ║          Dynatrace AI Observability Workshop - Environment Config         ║
# ╚══════════════════════════════════════════════════════════════════════════╝
#
# 📝 INSTRUCTIONS:
#    1. Replace the placeholder values below with your actual values
#    2. Your instructor will provide DT_ENDPOINT, DT_API_TOKEN, and WORKSHOP_TOKEN
#    3. Azure OpenAI credentials are fetched automatically using the workshop token
#
# ═══════════════════════════════════════════════════════════════════════════

# ┌──────────────────────────────────────────────────────────────────────────┐
# │ ATTENDEE CONFIGURATION - REQUIRED                                        │
# │ Enter your initials or unique identifier (e.g., jsmith, user01)          │
# └──────────────────────────────────────────────────────────────────────────┘
ATTENDEE_ID=your-initials-here

# ┌──────────────────────────────────────────────────────────────────────────┐
# │ DYNATRACE CONFIGURATION - PROVIDED BY INSTRUCTOR                          │
# └──────────────────────────────────────────────────────────────────────────┘
DT_ENDPOINT=https://YOUR_ENV.live.dynatrace.com/api/v2/otlp
DT_API_TOKEN=your-dynatrace-api-token

# ┌──────────────────────────────────────────────────────────────────────────┐
# │ AZURE OPENAI CONFIGURATION - FETCHED AUTOMATICALLY                       │
# │ (Credentials are fetched using your workshop token during setup)          │
# └──────────────────────────────────────────────────────────────────────────┘
# Azure OpenAI credentials will be added here automatically when you enter
# your workshop token during Codespace setup.
#
# If you need to re-fetch credentials, run:
#   bash .devcontainer/fetch-secrets.sh

# ┌──────────────────────────────────────────────────────────────────────────┐
# │ APPLICATION CONFIGURATION - DEFAULT VALUES                                │
# └──────────────────────────────────────────────────────────────────────────┘
APP_HOST=0.0.0.0
APP_PORT=8000
